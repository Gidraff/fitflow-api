apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitflow-frontend.fullname" . }}
  labels:
    {{- include "fitflow-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fitflow-frontend.selectorLabels" . | nindent 4 }}
