apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fitflow-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "fitflow-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fitflow-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
