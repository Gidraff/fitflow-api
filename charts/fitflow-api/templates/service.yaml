apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitflow-api.fullname" . }}
  labels:
    {{- include "fitflow-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fitflow-api.selectorLabels" . | nindent 4 }}
